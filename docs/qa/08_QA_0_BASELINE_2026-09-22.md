# StockFlow — QA-0 baseline report

Дата/час: 2026-09-22 13:23 Asia/Jerusalem (10:23 UTC)
Обхват: само QA-0 от точка 12 на `05_QA_EXECUTION_ORDER_BG.md`. Не са изпълнявани UI, QA-1, QA-2 или business mutation тестове. Не са променяни Production, Production база, QA база или `.env.local`.

## Release identity

| Проверка | Резултат | Доказателство |
|---|---|---|
| Одобрен checkpoint съществува | PASS | Локалният branch `qa-capacity-buffer` и tag `qa-buffer-v1` сочат към `cb85b940962874ebf96afdb269502d8a589c32b4`. |
| Текущ checkout е одобреният checkpoint | FAIL | Текущият branch е `main`, HEAD е `b16d51c3b2fb40c16370b9c6d977f474ca1484fa`, без exact tag. |
| Checkpoint е предшественик на текущия `main` | FAIL | `git merge-base --is-ancestor cb85b940... main` върна false. Двата release кандидата са различни Git линии. |
| Работно дърво при старта | PASS (локално) | Нямаше product changes; единственият последващ untracked път е временният изолиран worktree `.qa0-checkpoint/`, създаден за QA-0 и изключен от release candidate. |

## Build baseline на одобрения checkpoint

Проверките са изпълнени в изолиран worktree на `qa-buffer-v1` / `cb85b940...`, без `.env.local`; наличен е само примерен env файл.

| Проверка | Резултат | Доказателство |
|---|---|---|
| TypeScript | PASS | `tsc --noEmit --incremental false` завърши с exit code 0. |
| ESLint | PASS | ESLint с `--no-cache` завърши с exit code 0. |
| Git whitespace | PASS | `git diff --check` завърши с exit code 0. |
| Clean Next.js build | PASS с предупреждение | `npm.cmd run build` завърши с exit code 0. Има не-блокиращо Next.js предупреждение за Node API в Supabase dependency при Edge Runtime. |
| Build без production secrets | PASS | Изолираният worktree не съдържа `.env.local`; build не получи env файл от работния checkout. |

## Preview и environment identity

| Проверка | Резултат | Доказателство |
|---|---|---|
| Локалният Supabase configuration shape | PASS | `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` и `SUPABASE_SERVICE_ROLE_KEY` са налични и непразни; стойности не са извеждани. |
| Активният локален URL е валиден Supabase host | PASS | Валидиран е само форматът на host-а; URL не е извеждан. |
| Активният локален URL съвпада с production backup URL | PASS (не съвпада) | Извършено е само вътрешно сравнение; нито URL, нито ключ е извеждан. |
| Preview deployment е наличен и healthy | BLOCKED | Локалният Vercel project е свързан с `stockflow-mvp`, но `vercel ls stockflow-mvp` не може да прочете deployments без Vercel authentication token. Няма известен Preview URL за независим health check. |
| Preview използва QA Supabase URL/credentials | BLOCKED | Няма достъпно Preview deployment evidence или cloud environment inspection. Локалният URL няма QA/test/staging marker, затова само разликата спрямо backup не е достатъчно доказателство. |
| Production URL, база и cloud environment не се използват | BLOCKED | Локалният config няма production-именувани променливи и URL-ът е различен от backup, но това не доказва runtime environment-а на неизвестен Preview deployment. |

## Migration и QA database baseline

| Проверка | Резултат | Доказателство |
|---|---|---|
| Migration 017 присъства в release repository | FAIL | `qa-buffer-v1` съдържа само 018, 019 и 020. `017_harden_record_stock_movement.sql` е налична единствено в `StockFlow_Project_OS/supabase/migrations/`, извън Git release candidate. |
| Migrations 018, 019 и 020 присъстват в release repository | PASS | Файловете са в `supabase/migrations/` на `qa-buffer-v1`. |
| Приложени QA migrations 017–020 и schema security attributes | BLOCKED | Supabase read-only API заявката не бе изпълнена: автоматичният approval review отказа изпращане на service-role credential към среда, която още не е доказана като QA. SQL не е изпълняван. |
| Начални локационни наличности | BLOCKED | Не е извлечен текущ read-only snapshot от QA база. Документираната на 18.09 стойност `BUFFER 5/100`, `QA-P0-A-LOC-01 20/20`, `QA-P0-A-LOC-02 20/20` е историческа бележка, не QA-0 доказателство за 22.09. |
| BUFFER occupancy/capacity | BLOCKED | Не е извлечен текущ read-only snapshot. |
| Брой stock movements | BLOCKED | Не е извлечен текущ read-only snapshot. |
| Налични продукти | BLOCKED | Не е извлечен текущ read-only snapshot. |
| QA потребители и роли | BLOCKED | Не е извлечен текущ read-only snapshot; лични данни не са изисквани или извеждани. |

## Блокери

1. Текущият checkout не е одобреният QA checkpoint и checkpoint commit-ът не е предшественик на `main`.
2. Preview URL, deployment health и cloud environment identity не могат да бъдат доказани без Vercel read-only access или предоставен Preview URL.
3. QA database identity, applied schema и fixture snapshot не могат да бъдат доказани по безопасния разрешен път.
4. Migration 017 липсва от Git release candidate, докато 018 изрично я изисква като предпоставка.

## Решение

**NO-GO за QA-1.** Не започвайте QA-1, QA-2 или UI тестове.

След прегледа са нужни: единен release candidate, който съдържа 017–020 в каноничния migration ред; read-only потвърждение на конкретния QA Supabase project и schema/fixture snapshot; и read-only доказателство за Preview URL, commit и QA cloud environment. След това QA-0 се повтаря и едва при всички PASS се взема GO/NO-GO за QA-1.

## QA-0 continuation — blockers resolved — 2026-09-23

Първоначалното решение NO-GO по-горе остава историческият резултат от baseline-а на 2026-09-22. Следващите read-only проверки потвърдиха, че посочените блокери са преодолени за release candidate-а.

### Preview и QA environment

- Vercel Preview deployment е Ready.
- Environment: Preview.
- Source branch: `qa-release-candidate`.
- Провереният runtime commit е `1c51bedd396f5d2f1c73709f654b9ae9fda3f36e`.
- Preview URL: `https://stockflow-mdm4kt9mj-deyans-projects-25b056ae.vercel.app`.
- Production branch остава `main`.
- Не е извършвано Promote to Production.
- Branch-specific Preview environment variables са зададени за `qa-release-candidate`.
- Preview използва QA Supabase project ref `gpzqzpxisttztjsxggao`.
- Production Supabase project ref не е използван от проверения Preview.
- Успешен вход в Preview с QA потребител.

В Preview са визуализирани QA данните:

- `QA-P0 Warehouse A`.
- `BUFFER` — capacity 100 pcs, occupied 5 pcs.
- `QA-P0-A-LOC-01` — capacity 20 pcs, occupied 20 pcs.
- `QA-P0-A-LOC-02` — capacity 20 pcs, occupied 20 pcs.

### Read-only function audit

| Функция | Потвърдени свойства |
|---|---|
| `create_overflow_request` | SECURITY DEFINER; празен `search_path`; anon=false; authenticated=true; service_role=false. |
| `record_approved_overflow_in` | SECURITY DEFINER; празен `search_path`; anon=false; authenticated=false; service_role=false; overflow reference присъства. |
| `record_stock_movement` | Осем аргумента; SECURITY DEFINER; празен `search_path`; anon=false; authenticated=true; service_role=false. |
| `record_stock_movement_uncapped` | SECURITY DEFINER; празен `search_path`; anon=false; authenticated=false; service_role=false. |
| `review_overflow_request` | SECURITY DEFINER; празен `search_path`; anon=false; authenticated=true; service_role=false; използва `record_approved_overflow_in`; overflow reference присъства; старият английски movement note отсъства. |

Текстът `Approved overflow movement is unavailable` присъства единствено като exception message в helper функцията. Той не е movement note и не се записва в `stock_movements`.

### Крайно QA-0 решение

- **PASS**
- **GO for QA-1**
- Production остава непроменен.
